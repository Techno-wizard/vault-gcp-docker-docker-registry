# gentate gcp docker repo push pull service account key
path "gcp/key/docker-repo-push-pull"
{
	capabilities = ["read"]
}

# gentate gcp docker repo push pull oauth token
path "gcp/token/docker-repo-push-pull-token"
{
	capabilities = ["read"]
}